# Android release ledger

This ledger records artifacts uploaded to Google Play. It contains no signing
secrets. `pubspec.yaml` remains the source for `versionName+versionCode`.

No Play artifacts have been uploaded for `com.thorne.mcqquizzer` yet.

| Version | Code | Git commit | AAB SHA-256 | Track | Upload date (UTC) | Result/notes |
| --- | ---: | --- | --- | --- | --- | --- |
| _First release pending_ | — | — | — | — | — | Play package registration and Owner upload key required. |

Before adding a row, verify that the code is greater than every artifact ever
uploaded to any track. Record the exact commit and hash before upload; never
replace an existing row with a different artifact using the same version code.
