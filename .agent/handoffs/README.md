# Handoffs

Create one Markdown file per meaningful handoff. Suggested filename:

`YYYY-MM-DD-agent-short-topic.md`

Each handoff should state branch, goal, changed files, validation performed, APK/run reference when available, unresolved items, and what the next agent/Owner should do.

Do not store secrets or raw credential-bearing logs here.
