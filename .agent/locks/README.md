# Lightweight Locks

Use only when two agents could collide in the same subsystem or files. A lock is a short Markdown file naming the owner agent, branch, files/area, reason, and when it can be released.

Locks are coordination notices, not hard enforcement. Remove them when the overlap risk is gone.
