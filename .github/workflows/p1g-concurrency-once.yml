name: P1G Concurrency Once

on:
  push:
    branches:
      - dedal/history-repair-v1
    paths:
      - '.p1g-concurrency-trigger'

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dedal/history-repair-v1
          fetch-depth: 0
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: '3.47.2'
          cache: true
      - name: Apply bounded concurrency integration
        run: python .agent/p1g_concurrency_integrate.py
      - name: Format and analyze
        run: |
          dart format lib/services/generation_plan.dart lib/services/ai_generation_service.dart
          flutter pub get
          flutter analyze --no-fatal-infos --no-fatal-warnings
      - name: Commit integration
        run: |
          git config user.name "DEDAL Automation"
          git config user.email "85703541+Ye-Shwethway@users.noreply.github.com"
          git add lib/services/generation_plan.dart lib/services/ai_generation_service.dart
          git commit -m "feat: add bounded parallel profile generation"
          git push origin HEAD:dedal/history-repair-v1
